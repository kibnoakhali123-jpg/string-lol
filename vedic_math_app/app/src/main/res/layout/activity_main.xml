<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0B0B0B"
    android:keepScreenOn="true">

    <!-- Main WebView that loads your meme app -->
    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0B0B0B" />

    <!-- Optional: Loading indicator (appears while WebView loads) -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_alignParentTop="true"
        android:max="100"
        android:progressTint="#FF99CC"
        android:visibility="gone" />

    <!-- Optional: Error message (appears if loading fails) -->
    <TextView
        android:id="@+id/errorText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Failed to load content"
        android:textColor="#FF99CC"
        android:textSize="16sp"
        android:visibility="gone" />

</RelativeLayout>